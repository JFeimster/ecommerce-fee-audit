version: 1.0.0
modes:
  autonomous: {trigger_rules: enabled_rule, approval_state: preauthorized, stored_authorization: required, wait_node_behavior: none, timeout_behavior: safe_stop, escalation: configured_threshold, cancellation: disable_rule, resume_behavior: checkpoint, audit_event: job.started, user_notification: configured}
  confirm_once: {trigger_rules: matching_preauthorization, approval_state: stored, stored_authorization: required, wait_node_behavior: initial_only, timeout_behavior: safe_stop, escalation: configured_threshold, cancellation: revoke_authorization, resume_behavior: checkpoint, audit_event: entitlement.updated, user_notification: configured}
  confirm_each_time: {trigger_rules: explicit_request, approval_state: per_execution, stored_authorization: false, wait_node_behavior: required, timeout_behavior: expire_and_stop, escalation: manual_queue, cancellation: stop, resume_behavior: new_request, audit_event: job.queued, user_notification: required}
  human_review_required: {trigger_rules: configured_exception, approval_state: reviewer, stored_authorization: false, wait_node_behavior: required, timeout_behavior: escalate, escalation: internal_operator, cancellation: stop, resume_behavior: reviewer_release, audit_event: system.manual_intervention_requested, user_notification: configured}
  internal_only: {trigger_rules: internal_event, approval_state: internal_policy, stored_authorization: internal, wait_node_behavior: optional, timeout_behavior: internal_queue, escalation: administrator, cancellation: internal_stop, resume_behavior: operator, audit_event: job.started, user_notification: internal_only}
