version: 1.0.0
forms:
  free_audit_starter_intake: {purpose: free audit starter intake, fields: [name, email, platform, audit_period], required_fields: [name, email, platform], sensitive_fields: [], redaction_requirements: no_secrets_or_financial_identifiers, destination: wix_crm, crm_labels: [platform_fee_audit_lead, free_starter], operation_ids: [createAudit], emitted_events: [audit.created], entitlement_effect: starter_activation_pending, execution_mode: confirm_once, follow_up_workflow: onboarding}
  paid_audit_onboarding: {purpose: paid audit onboarding, fields: [name, email, product, platforms, audit_period], required_fields: [name, email, product], sensitive_fields: [], redaction_requirements: no_secrets_or_financial_identifiers, destination: wix_crm, crm_labels: [ecommerce_seller], operation_ids: [createAudit], emitted_events: [audit.created], entitlement_effect: onboarding_required, execution_mode: confirm_once, follow_up_workflow: onboarding}
  managed_service_intake: {purpose: managed service request, fields: [name, email, service, channels, goals], required_fields: [name, email, service], sensitive_fields: [], redaction_requirements: no_secrets_or_financial_identifiers, destination: wix_crm, crm_labels: [managed_service], operation_ids: [createManagedServiceTask], emitted_events: [audit.created], entitlement_effect: none, execution_mode: confirm_once, follow_up_workflow: operator_review}
  partner_license_application: {purpose: partner license application, fields: [name, email, organization, partner_type], required_fields: [name, email, organization, partner_type], sensitive_fields: [], redaction_requirements: no_secrets_or_financial_identifiers, destination: wix_crm, crm_labels: [partner_license], operation_ids: [createManagedServiceTask], emitted_events: [audit.created], entitlement_effect: none, execution_mode: confirm_once, follow_up_workflow: partner_review}
  connector_request: {purpose: connector request, fields: [connector_id, account_context], required_fields: [connector_id], sensitive_fields: [], redaction_requirements: no_credentials, destination: connector_queue, crm_labels: [], operation_ids: [beginConnectorSession], emitted_events: [connector.session_started], entitlement_effect: none, execution_mode: confirm_each_time, follow_up_workflow: connector_authorization}
  dispute_support_request: {purpose: dispute support request, fields: [platform, dispute_reference, deadline], required_fields: [platform], sensitive_fields: [dispute_reference], redaction_requirements: mask_references, destination: dispute_queue, crm_labels: [dispute_support], operation_ids: [createDisputeCase], emitted_events: [dispute.case_created], entitlement_effect: none, execution_mode: confirm_once, follow_up_workflow: dispute_intake}
  funding_readiness_lead: {purpose: funding readiness lead, fields: [name, email, business_context], required_fields: [name, email], sensitive_fields: [], redaction_requirements: no_ssn_or_bank_data, destination: wix_crm, crm_labels: [funding_readiness_lead], operation_ids: [createFundingReadinessLead], emitted_events: [funding.lead_created], entitlement_effect: none, execution_mode: confirm_once, follow_up_workflow: funding_follow_up}
  cancellation_request: {purpose: cancellation request, fields: [product, reason], required_fields: [product], sensitive_fields: [], redaction_requirements: no_secrets, destination: wix_crm, crm_labels: [cancellation_requested], operation_ids: [updateAudit], emitted_events: [entitlement.updated], entitlement_effect: cancellation_pending, execution_mode: confirm_once, follow_up_workflow: cancellation_recovery}
  upgrade_request: {purpose: upgrade request, fields: [current_product, requested_product], required_fields: [requested_product], sensitive_fields: [], redaction_requirements: no_secrets, destination: wix_crm, crm_labels: [upgrade_candidate], operation_ids: [updateAudit], emitted_events: [entitlement.upgrade_required], entitlement_effect: upgrade_pending, execution_mode: confirm_once, follow_up_workflow: upgrade_review}
