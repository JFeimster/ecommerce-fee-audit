version: 1.0.0
status: provisional_and_configurable
classes:
  source_files: {default_retention: configurable_minimum_90_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_request, disconnection_behavior: stop_new_collection, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: retain_only_if_needed, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  normalized_records: {default_retention: configurable_minimum_365_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_request, disconnection_behavior: no_new_sync, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: rederive_or_delete, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  derived_findings: {default_retention: configurable_minimum_365_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_request, disconnection_behavior: retain_per_audit_choice, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: delete_with_source_when_required, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  connector_metadata: {default_retention: configurable_minimum_90_days, product_tier_overrides: allowed, deletion_behavior: remove_on_disconnect_when_allowed, disconnection_behavior: revoke_and_minimize, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: not_applicable, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  sync_cursors: {default_retention: configurable_minimum_90_days, product_tier_overrides: allowed, deletion_behavior: delete_on_disconnect_when_allowed, disconnection_behavior: invalidate, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: not_applicable, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  webhook_payload_summaries: {default_retention: configurable_minimum_30_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_request, disconnection_behavior: stop_accepting, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: no_raw_payload_retain_by_default, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  audit_logs: {default_retention: configurable_minimum_365_days, product_tier_overrides: allowed, deletion_behavior: policy_controlled, disconnection_behavior: retain_minimum_audit_trace, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: no_sensitive_payloads, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  quarantined_files: {default_retention: configurable_minimum_30_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_after_resolution, disconnection_behavior: unchanged, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: not_processed_until_release, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  dead_letter_records: {default_retention: configurable_minimum_30_days, product_tier_overrides: allowed, deletion_behavior: secure_delete_after_resolution, disconnection_behavior: unchanged, legal_hold: placeholder, user_requested_deletion: supported_subject_to_hold, derived_data_handling: replay_only_when_authorized, backup_handling: scheduled_expiry, sensitive_data_minimization: required}
  temporary_upload_sessions: {default_retention: configurable_short_expiry, product_tier_overrides: not_applicable, deletion_behavior: automatic_expiry, disconnection_behavior: invalidate, legal_hold: not_applicable, user_requested_deletion: automatic, derived_data_handling: not_applicable, backup_handling: minimal, sensitive_data_minimization: required}
