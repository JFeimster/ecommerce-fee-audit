version: 1.0.0
modes:
  manual: {trigger: user_upload_or_operator_request, default_date_window: user_selected_period, cursor_behavior: none, pagination_behavior: not_applicable, checkpointing: file_manifest, duplicate_prevention: checksum_and_record_keys, timeout_policy: standard, retry_policy: user_retries, partial_completion_behavior: preserve_completed_records, cancellation_behavior: stop_future_work, audit_logging: required, emitted_events: [upload.completed, job.completed], fallback_behavior: manual_review}
  on_demand: {trigger: explicit_user_request, default_date_window: rolling_30_days, cursor_behavior: provider_cursor_when_available, pagination_behavior: provider_managed, checkpointing: required, duplicate_prevention: idempotency_key_and_cursor, timeout_policy: standard, retry_policy: exponential_standard, partial_completion_behavior: retain_checkpoint, cancellation_behavior: cancel_remaining_pages, audit_logging: required, emitted_events: [connector.sync_requested, connector.sync_completed], fallback_behavior: manual_upload}
  scheduled: {trigger: enabled_schedule, default_date_window: since_last_checkpoint, cursor_behavior: required_when_supported, pagination_behavior: provider_managed, checkpointing: required, duplicate_prevention: cursor_and_source_record_id, timeout_policy: standard, retry_policy: exponential_standard, partial_completion_behavior: retry_unfinished_pages, cancellation_behavior: disable_schedule, audit_logging: required, emitted_events: [connector.sync_started, connector.sync_completed], fallback_behavior: on_demand}
  incremental: {trigger: sync_cursor, default_date_window: since_last_checkpoint, cursor_behavior: required, pagination_behavior: provider_managed, checkpointing: required, duplicate_prevention: cursor_and_source_record_id, timeout_policy: standard, retry_policy: exponential_standard, partial_completion_behavior: retain_checkpoint, cancellation_behavior: invalidate_pending_cursor, audit_logging: required, emitted_events: [connector.sync_started, connector.sync_partially_completed], fallback_behavior: historical_backfill}
  historical_backfill: {trigger: explicit_authorized_request, default_date_window: user_selected_period, cursor_behavior: provider_dependent, pagination_behavior: provider_managed, checkpointing: required, duplicate_prevention: period_and_source_record_id, timeout_policy: extended, retry_policy: controlled_retry, partial_completion_behavior: require_period_label, cancellation_behavior: retain_completed_periods, audit_logging: required, emitted_events: [connector.sync_started, connector.sync_partially_completed], fallback_behavior: manual_upload}
  webhook_triggered: {trigger: verified_webhook, default_date_window: event_scoped, cursor_behavior: event_id, pagination_behavior: follow_up_job_if_needed, checkpointing: required, duplicate_prevention: webhook_event_id, timeout_policy: short, retry_policy: webhook_policy, partial_completion_behavior: queue_async_job, cancellation_behavior: reject_future_events_after_disconnect, audit_logging: required, emitted_events: [connector.webhook_received, connector.sync_requested], fallback_behavior: scheduled}
  hybrid: {trigger: schedule_with_verified_webhooks, default_date_window: since_last_checkpoint, cursor_behavior: required_when_supported, pagination_behavior: provider_managed, checkpointing: required, duplicate_prevention: cursor_event_id_and_source_record_id, timeout_policy: standard, retry_policy: exponential_standard, partial_completion_behavior: reconcile_next_schedule, cancellation_behavior: disable_schedule_and_webhook, audit_logging: required, emitted_events: [connector.sync_started, connector.sync_completed], fallback_behavior: on_demand}
