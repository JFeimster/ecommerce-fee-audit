version: 1.0.0
classes:
  low: {request_budget: configurable_low, concurrency: 1, burst_behavior: queue, retry_after_handling: required, backoff_policy: exponential_with_jitter, provider_header_handling: honor_when_present, job_scheduling_behavior: serialized, product_tier_override: configurable, connector_specific_override: allowed}
  standard: {request_budget: configurable_standard, concurrency: configurable, burst_behavior: queue, retry_after_handling: required, backoff_policy: exponential_with_jitter, provider_header_handling: honor_when_present, job_scheduling_behavior: checkpointed, product_tier_override: configurable, connector_specific_override: allowed}
  high_volume: {request_budget: configurable_high, concurrency: configurable, burst_behavior: queue_and_throttle, retry_after_handling: required, backoff_policy: exponential_with_jitter, provider_header_handling: honor_when_present, job_scheduling_behavior: partitioned, product_tier_override: configurable, connector_specific_override: allowed}
  provider_managed: {request_budget: provider_defined, concurrency: provider_defined, burst_behavior: provider_defined, retry_after_handling: required, backoff_policy: provider_defined, provider_header_handling: required, job_scheduling_behavior: provider_aware, product_tier_override: not_applicable, connector_specific_override: required}
  manual_only: {request_budget: not_applicable, concurrency: not_applicable, burst_behavior: not_applicable, retry_after_handling: not_applicable, backoff_policy: user_retry, provider_header_handling: not_applicable, job_scheduling_behavior: user_initiated, product_tier_override: configurable, connector_specific_override: allowed}
connector_classes: {manual_upload: manual_only, google_drive: standard, google_sheets: standard, shopify: provider_managed, stripe: provider_managed, quickbooks: provider_managed, xero: provider_managed, amazon_seller_central: provider_managed, walmart_marketplace: provider_managed, tiktok_shop: provider_managed, google_ads: provider_managed, meta_ads: provider_managed, generic_partner_api: low, generic_webhook: high_volume, cloud_storage: standard, email_attachment: manual_only, internal_agent: standard, custom_gpt: low}
