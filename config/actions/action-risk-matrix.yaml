version: 1.0.0
profiles:
  public_read: {operation_ids: [getPublicFeeTaxonomy, getAuditTemplateSchema, getDisputeChecklist, getFundingDocumentChecklist, getProductCatalog, getSupportedConnectors, getPricingSummary, getPublicSampleAudit, getPublicPromptPack], risk_class: read_only, execution_mode: autonomous, required_authorization: no_auth, confirmation_rule: none, human_review_trigger: never_for_public_resource, idempotency: none, audit_log: public_access, retry: bounded_read_retry, rollback: none, data_access: public_safe, destinations: [public_resource]}
  internal_writes: {operation_ids: [createAudit, updateAudit, finalizeUpload, createDisputeCase, createFundingReadinessLead, createFundingApplication, createCorrectiveAction], risk_class: record_write, execution_mode: confirm_once, required_authorization: api_key, confirmation_rule: configured_confirmation, human_review_trigger: threshold_or_user_setting, idempotency: required, audit_log: required, retry: idempotent_retry, rollback: compensating_record, data_access: authorized, destinations: [internal_agent, hubspot, notion]}
  submissions: {operation_ids: [generateAndSendReport, submitDisputePackage, submitFundingApplication, sendDocumentRequest, startFollowUpSequence, startFundingFollowUpSequence, requestMissingDisputeEvidence], risk_class: external_submission, execution_mode: confirm_each_time, required_authorization: api_key, confirmation_rule: payload_confirmation, human_review_trigger: threshold_or_provider_rule, idempotency: required, audit_log: required, retry: no_blind_retry, rollback: compensating_message_or_case, data_access: minimum_necessary, destinations: [email, marketplace_portal, processor_portal, partner_api, n8n]}
  automation: {operation_ids: [runAudit, assembleDisputePackage, validateDisputeEvidence, trackDisputeStatus, routeFundingApplication, runAutomatedQualification, runAutomatedRouting, syncConnectedPlatform], risk_class: automated_decision, execution_mode: autonomous, required_authorization: stored_authorization, confirmation_rule: enabled_workflow, human_review_trigger: confidence_or_limit_trigger, idempotency: required, audit_log: required, retry: idempotent_retry, rollback: compensating_workflow, data_access: authorized, destinations: [internal_agent, moonshine_funnel, processor_portal], confidence_threshold: configurable}
  connector: {operation_ids: [requestUploadSession, beginConnectorSession, getConnectorStatus, requestConnectorSync, disconnectConnector, refreshConnectorAuthorization, getAvailableAccounts, selectConnectorAccount, requestBankConnection], risk_class: financial_data_access, execution_mode: confirm_once, required_authorization: oauth_or_api_key, confirmation_rule: connector_scope_confirmation, human_review_trigger: provider_or_account_mismatch, idempotency: required, audit_log: required, retry: provider_safe_retry, rollback: disconnect_or_manual_upload, data_access: financial_minimum_necessary, destinations: [cloud_storage, webhook, partner_api]}
  internal_control: {operation_ids: [approveLowConfidenceDecision, overrideRoutingDecision, resolveDataGap, requeueFailedJob, markDisputeReviewed, markApplicationReviewed, escalateCase, escalateDisputeCase, getInternalAuditTrail, createManagedServiceTask, approveAuditWorkflow, saveAuditReport, generateAuditExport, getAudit, getAuditFindings], risk_class: restricted, execution_mode: internal_only, required_authorization: internal_only, confirmation_rule: internal_policy, human_review_trigger: configured, idempotency: required, audit_log: required, retry: idempotent_or_no_blind_retry, rollback: compensating_record, data_access: internal_authorized, destinations: [internal_agent]}
