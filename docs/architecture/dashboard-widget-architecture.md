# Dashboard and Widget Architecture

`site/app/` is a static, accessible vanilla-JS shell that consumes public local JSON and labels all actions simulated. It plans to call existing shared API routes later without creating routes. Wix Members Area can launch a future signed handoff to this Vercel dashboard. `site/embed/` is a portable launcher for Wix, WordPress, and partner pages; it never accepts sensitive files.
