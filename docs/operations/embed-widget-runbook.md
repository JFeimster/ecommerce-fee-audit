# Embed Widget Runbook

Use the widget files as a portable bundle, keep dimensions at or below the recommended 1200px × 940px display envelope, set only public configuration attributes, test keyboard access and compact mode, and direct uploads to the future secure app—not the widget.
