# n8n Versioning and Rollback Runbook

Name workflow versions, export before edits, create an inactive duplicate, test it, promote only after approval, retain an audit trail, document breaking changes and dependencies, and rollback by restoring the prior inactive export before reactivation.
