# Static Dashboard Shell

This vanilla HTML/CSS/JS shell uses only public fictional local JSON. It demonstrates state and navigation; it does not authenticate members, upload files, call APIs, connect providers, submit disputes, submit funding applications, or make decisions.
