const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..','..');
test('dashboard static application has local assets and public data without secrets',()=>{for(const file of ['site/app/index.html','site/app/app.js','site/app/styles.css','site/data/public-product-catalog.json'])assert.ok(fs.existsSync(path.join(root,file)),`missing ${file}`);const html=fs.readFileSync(path.join(root,'site/app/index.html'),'utf8');assert.match(html,/styles\.css/);assert.match(html,/app\.js/);assert.ok(!/site\/api\//.test(html));JSON.parse(fs.readFileSync(path.join(root,'site/data/public-product-catalog.json'),'utf8'));});
