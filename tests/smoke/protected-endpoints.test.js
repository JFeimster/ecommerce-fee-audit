const test=require('node:test');const assert=require('node:assert/strict');const{request,response}=require('../helpers/http-mocks');const routes={intake:['../../site/api/intake','createAudit','POST'],uploads:['../../site/api/uploads','requestUploadSession','POST'],audits:['../../site/api/audits','runAudit','POST'],exports:['../../site/api/exports','generateAuditExport','POST'],connectors:['../../site/api/connectors','getConnectorStatus','GET'],'oauth-callback':['../../site/api/oauth-callback','refreshConnectorAuthorization','POST'],jobs:['../../site/api/jobs','runAutomatedQualification','POST']};
test('protected shared routes fail safely without credentials',()=>{for(const [name,[module,operationId,method]] of Object.entries(routes)){const res=response();require(module)(request({method,query:{operationId}}),res);assert.ok([401,503].includes(res.statusCode),`${name} did not fail closed`);assert.ok(res.body.error.request_id);assert.ok(!JSON.stringify(res.body).match(/stack|secret|test-key/i));}});
