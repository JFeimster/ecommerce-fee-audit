const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..','..');
test('embed widget is portable, privacy-aware, and does not accept binary uploads',()=>{for(const file of ['site/embed/platform-fee-audit-widget.html','site/embed/platform-fee-audit-widget.css','site/embed/platform-fee-audit-widget.js','site/embed/README.md'])assert.ok(fs.existsSync(path.join(root,file)),`missing ${file}`);const text=['platform-fee-audit-widget.html','platform-fee-audit-widget.css','platform-fee-audit-widget.js'].map(f=>fs.readFileSync(path.join(root,'site/embed',f),'utf8')).join('\n');assert.match(text,/1200px/);assert.match(text,/940px/);assert.match(text,/(privacy|secure)/i);assert.ok(!/type=["']file/i.test(text));});
