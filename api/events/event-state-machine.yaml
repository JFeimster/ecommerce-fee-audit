version: 1.0.0
defaults: {invalid_transition: reject_and_audit_log, reentry: requires_explicit_retry_or_reopen}
machines:
  audit: {initial: created, states: [created, queued, started, completed, partially_completed, failed, cancelled, review_requested, approved, reopened], terminal: [completed, cancelled], retryable: [failed], transitions: ['created>queued','queued>started','started>completed','started>partially_completed','started>failed','failed>queued','completed>reopened','reopened>queued']}
  upload: {initial: session_requested, states: [session_requested, completed, failed, expired], terminal: [completed, expired], retryable: [failed], transitions: ['session_requested>completed','session_requested>failed','session_requested>expired']}
  connector_sync: {initial: sync_requested, states: [sync_requested, sync_started, sync_completed, sync_partially_completed, sync_failed, disconnected], terminal: [sync_completed, disconnected], retryable: [sync_failed], transitions: ['sync_requested>sync_started','sync_started>sync_completed','sync_started>sync_partially_completed','sync_started>sync_failed','sync_failed>sync_requested']}
  funding_application: {initial: application_created, states: [application_created, application_validated, application_incomplete, application_submitted, application_accepted, provider_decision_recorded, approved, declined, funded], terminal: [declined, funded], retryable: [application_incomplete], transitions: ['application_created>application_validated','application_validated>application_submitted','application_submitted>application_accepted','application_accepted>provider_decision_recorded','provider_decision_recorded>approved','approved>funded','provider_decision_recorded>declined']}
  dispute_case: {initial: case_created, states: [case_created, evidence_requested, evidence_received, package_assembled, package_validated, package_submitted, recovery_recorded, rejected, escalated, closed], terminal: [closed], retryable: [rejected], transitions: ['case_created>evidence_requested','evidence_requested>evidence_received','evidence_received>package_assembled','package_assembled>package_validated','package_validated>package_submitted','package_submitted>recovery_recorded','recovery_recorded>closed','package_submitted>rejected','rejected>escalated','escalated>closed']}
  job: {initial: created, states: [created, queued, started, partially_completed, completed, failed, retry_scheduled, requeued, cancelled, timed_out, dead_lettered], terminal: [completed, cancelled, dead_lettered], retryable: [failed, timed_out], transitions: ['created>queued','queued>started','started>completed','started>failed','failed>retry_scheduled','retry_scheduled>requeued','requeued>queued','timed_out>retry_scheduled','failed>dead_lettered']}
